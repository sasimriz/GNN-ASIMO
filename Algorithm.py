Algorithm 1. Prediction of Passenger traveled for food attraction
Input: The features of 06 subway stations and food POI in WDK & CBD.
		begin
		for i =1
		 	choose the station Si until Si+4
		 	for all S ? CBD, where 
		 	for all P, where L<k
		 	if  (R =  r¬max + D = d¬min)
		 	Choice = CID 
		 	true;
		 	Edge ? weight (AI), Add Node
		 	elif ((R = r¬max + D = d¬max)
		 	Choice = CID 
		 	true;
		 	Edge ? weight (AI), Add Node
		 	elif (R = r¬¬min + D = d¬min)
		 	Choice = CID 
		 	true;
		 	Edge ? weight (AI), Add Node
		 	else  (R = r¬¬min + D = d¬max)
		 	Choice = CID 
		 	true;
		 	Edge ? weight (AI), Add Node
		Create P* for V by following the successors S as WDK
		Return Predict P*
Outputs: For all Station S, Predicted P for Visitors V.
